# Google Cloud launch and export-capacity prompt

Act as my senior Google Cloud architect, DevOps engineer and production reliability engineer. I am non-technical. Audit and fix only critical launch issues for Lekha Captions. Work promptly, preserve existing changes and customer data, and explain outcomes plainly. Do the authorized work rather than just recommending it.

First read AGENTS.md, CLAUDE.md, DEVELOPMENT_LOG.md, the deployment scripts, docs/GCP_DEPLOYMENT_2026-09-11.md and docs/GCP_LAUNCH_AUDIT_2026-09-09.md. Historical reports are leads, not current proof. Verify the real production architecture and DNS before assuming the Google Cloud migration is complete. The last inspection found public traffic on Netlify/Railway and a separate Google Cloud staging deployment.

Before any infrastructure change, show the authenticated account, project ID and number, regions, domains, frontend/API/worker releases and immutable image digests. Confirm the production target from evidence. If access is missing, say exactly what is blocked and continue useful local work. Never display secrets. Save the current configuration and a rollback plan before changing it.

Inspect the actual deployed resources: Cloud Run, Compute Engine VMs and managed instance groups, load balancers, Redis/Memorystore, storage, Firestore, Firebase Auth/App Check, IAM/service accounts, Secret Manager, firewall/VPC, DNS/TLS, monitoring, quotas, billing alerts, backups and CI/CD. For each relevant service report CPU, memory, scratch disk, concurrency, timeout, health probes, region, redundancy and autoscaling minimum/maximum/trigger. Mark settings verified ON, OFF, MISCONFIGURED, NOT APPLICABLE or UNVERIFIED. Do not infer live settings from environment examples.

Trace visitors, signup/login, projects, autosave, uploads, transcription, caption editing, exports, downloads, payment verification and webhooks. Fix critical authorization, duplicate billing, lost-job, data-loss and overload defects. Keep the API responsive while media work runs elsewhere. Production must never fall back to synchronous FFmpeg because the queue is unavailable. Historical latency must not cause every otherwise healthy API replica to fail its readiness probe.

The export objective is at most 300 seconds from clicking Export to an authorized downloadable result for a documented video-size, duration, resolution, FPS and effects envelope. Measure queue wait, preparation, render, persistence and completion separately, with p50/p95/p99, maximum and deadline misses. Do not claim a universal five-minute guarantee for arbitrary videos or unlimited arrivals. Define a feasible launch traffic envelope and cost ceiling from real measurements. Reject or defer excess demand gracefully before exhaustion; never silently discard accepted work.

Audit durable admission/outbox recovery, Redis loss, idempotency, duplicate clicks/deliveries, per-user fairness, bounded queue admission, retry limits/backoff, provider rate limits, dead-letter recovery, stale jobs, worker crashes, cancellation and graceful draining. Ensure terminal results and charging are idempotent. Separate transcription capacity from rendering so an export surge cannot starve new transcriptions. Plan queue migration with consumers ready before producers switch and drain the old queue. Do not blindly rename a live queue.

Benchmark representative real videos and then implement bounded worker autoscaling using backlog/work remaining and oldest-job age. Verify the metric publisher, permissions, freshness, scale-out, actual startup delay and safe scale-in. Account for in-flight jobs so workers are not killed when the pending queue empties. Reserve CPU/RAM for FFmpeg, Chromium, malware scanning and the OS; cap renders and threads per VM. Check regional CPU/IP/disk quotas. Configure API redundancy and scaling appropriate to its actual hosting platform. Do not assume Cloud Run frontend autoscaling also scales API VMs or workers.

Verify private Redis networking, a queue-safe eviction policy, HA/failover and recovery. Prepare any Redis migration before executing it. Verify storage IAM and customer isolation, expiring signed URLs, lifecycle cleanup and backups. Confirm whether S3 settings still override Google storage. For high upload/download volumes, use authorized direct/resumable object transfers with quarantine and scanning before processing. Keep original customer files safe. Check Firestore indexes, pagination, autosave debounce, transaction contention and restore capability.

Verify live payment mode, server-side signatures, amount/plan binding, duplicate/replayed webhooks, refund reconciliation, Firebase token validation and cross-user access rejection. Exercise disposable accounts, never real customer records. Use real minimal provider tests only where necessary; mock paid providers for high-volume tests.

Test first in isolated staging. Run a real short-video journey, then export bursts of 10, 25, 50, 100 and 250 distinct users, stopping escalation on unsafe resource use or poor results. Run representative API/editor/upload/download scenarios and a bounded soak. Measure actual render durations, worker counts, CPU/RAM/disk, queue age, errors and recovery. A health-endpoint benchmark or fake Redis test is not an export-capacity benchmark. Update stale test clients to support the current asynchronous transcription/export APIs before relying on their results.

Show truthful progress such as “Preparing your export”, “Rendering” and “Finalizing”, without exposing queue position. Handle retries, refresh and disconnection. Do not fabricate percentages, pretend rendering has started, or promise an unmeasured completion time.

Enable necessary alerts with verified delivery for unavailable services, missing workers, oldest waiting job, five-minute misses, failures/retries, Redis memory, scratch disk, quotas and spend. Budget alerts are not hard spending caps. Verify backup restore, immutable releases, health checks, rollback and migration acceptance before switching public traffic. Do not delete resources or data, blindly migrate databases, create expensive fleets, raise ongoing costs materially or run uncontrolled production stress tests. Prepare a concrete plan for actions outside this authorization.

Run affected backend/frontend/security tests, production-configured build, container build and controlled live acceptance. Deploy only a traceable release; verify frontend/API/workers match afterward. Do not mark local fixes as deployed.

Deliver a founder-readable verdict, architecture diagram, critical findings, changes and verification evidence, scaling table, measured capacity/deadline results, critical remaining gates, exact files/commands and rollback instructions. Mark unavailable evidence explicitly. Choose READY FOR PUBLIC LAUNCH only if the real configured system and measured workload support it; otherwise give the precise blockers.
